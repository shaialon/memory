#!/bin/bash

# call npm install or pip install according to the project stack, requirements, etc.
# if the project has a package.json, run npm install
# if the project has a requirements.txt, run pip install